import { StyleSheet } from "react-native";

export const usualStyle = StyleSheet.create({
  usualMode: {
    justifyContent: "space-between"
  },
  innerBlock: {
    width: "100%",
    height: "17%",
    flexDirection: "row",
    justifyContent: "space-between"
  },
  button: {
    width: "20%"
  }
});
