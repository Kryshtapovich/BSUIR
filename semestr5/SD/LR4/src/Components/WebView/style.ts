import { StyleSheet } from "react-native";

const webViewStyle = StyleSheet.create({
  closeButton: {
    position: "absolute",
    left: 15,
    bottom: 15,
    backgroundColor: "#2efdb7",
    width: 50,
    height: 50,
    borderRadius: 50,
    alignItems: "center",
    justifyContent: "center"
  }
});

export default webViewStyle;
