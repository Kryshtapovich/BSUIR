export default interface TimerModel {
  id: string;
  title: string;
  workMinutes: string;
  workSeconds: string;
  cooldownMinutes: string;
  cooldownSeconds: string;
  rounds: string;
  color: string;
}
